# CheapFlight
Application that allows users to find the cheapest flight between two selected cities. Users select a starting point and a destination from a given list of flights, and the app dynamically displays the lowest priced flight, along with a visual representation of all available options.
